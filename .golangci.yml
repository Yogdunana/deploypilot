version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
  settings:
    errcheck:
      exclude-functions:
        - fmt.Sscanf
  exclusions:
    rules:
      - linters:
          - errcheck
        path: _test\.go
